<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Depois do arranque, o fundo da Activity precisa ser simples. Se o
             WebView recompuser uma área ao abrir/fechar o teclado, nunca pode
             reaparecer a imagem de abertura por trás do editor. -->
        <item name="android:windowBackground">@color/appWindowBackground</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- Android 15+: exibição de ponta a ponta. As barras ficam
             transparentes e o conteúdo web usa env(safe-area-inset-*). -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>


    <!-- A imagem de abertura pertence somente à tela nativa de arranque. Não
         use android:background com o bitmap completo: esse drawable permanece
         preso à janela e pode ficar exposto durante relayouts do WebView. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/appWindowBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>